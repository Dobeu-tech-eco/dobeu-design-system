---
"@dobeu-private/components-react": patch
---

Initial `@dobeu-private/components-react` package: first primitives (`Button`, `Input`, `Card`, `Navbar`) with Vitest coverage, Figma Code Connect mappings backed by real design file node URLs (anchor file by default, overridable via `DOBEU_FIGMA_FILE_URL` / `DOBEU_FIGMA_NODE_*`), and `figma:resolve-nodes` helper script for published library component keys.
