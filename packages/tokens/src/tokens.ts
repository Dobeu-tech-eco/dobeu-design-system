import tokenData from "./tokens.json";

export const colorTokens = tokenData.colorTokens;
export const typographyTokens = tokenData.typographyTokens;
export const spacingTokens = tokenData.spacingTokens;
export const radiusTokens = tokenData.radiusTokens;
export const tldAccentTokens = tokenData.tldAccentTokens;
